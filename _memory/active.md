# Active Memory: Slidesmith — AI-first HTML Slides System

> Last updated: 2026-06-27
>
> **▶ v2 一站式闭环 · 制作阶段「皮肤选择流程」优化(2026-06-27,回到主线)**。用户澄清真实流程:**制作不在 Studio 做**,是 AI 用 `editorial-slides` 技能做初版,这一步要**选呈现风格(=22 皮)**,但技能里"皮肤呈现+选择"不顺。诊断:Step 0 虽存在但①看皮靠手动开 HTML ②无"真内容试皮" ③无场合→皮推荐 ④acrylic 没进技能。**优化(AI-first 可视化:推荐→看→试→定)**:① **build.py 修**:`--blank` 封面接 `--title/--sub`(新增 `{{COVER_TITLE}}/{{COVER_SUB}}` 占位+替换;无则回"主标题"占位)——这样"试皮"能渲**真封面**,也让起手式空 deck 带真标题;② **全 22 皮总览图** `gallery/theme-contact-sheet.png`(一张图浏览全部,AI 对话内直接贴;`scripts/build-contact-sheet.mjs` 可复现——**坑**:theme-showcase 的 iframe 是 `loading="lazy"`,截图前要强制 eager+重置 src 否则下半屏全黑);③ **SKILL.md Step 0 重写**=推荐(场合→皮表)→看(贴总览图)→试(`build.py <skin> /tmp/try-X.html --blank --title "真题"` 各渲一张截图并排)→定;补 acrylic、全 22 皮表、frontmatter skins 加 acrylic、skins_note、version 1.6.0。验证:build.py 改动 verify-skins 46/46;试皮实测 editorial/keynote-dark/acrylic 同一真标题三套封面真渲染。截图 `docs/screenshots/skinpick/{try-editorial,try-keynote-dark,try-acrylic}.png` + `gallery/theme-contact-sheet.png`。**未提交**。**闭环三段诊断(见对话 show_widget 图)**:制作=弱(本次优化选皮入口)·修改=强(Studio 已完整)·呈现=待补(演讲者视图只在旧 IR 模式,HTML-first 主流程缺)·讲稿同步=用户说"最后再接"。**下一步候选**:制作→修改的交接顺滑(生成完一键进 Studio)、或呈现态演讲者视图接主流程、或讲稿同步。
>
> **✅ 毛玻璃皮 acrylic(第 22 套)+ Fluent 风(2026-06-27,用户要「微软宣传片那种高大上毛玻璃卡片」)**。先派 general-purpose agent 调研 GitHub(返回见对话)——关键:agent **用我们工具链实测了 PDF 导出**,`backdrop-filter` 毛玻璃能正确光栅化进 PDF(最大风险点解除);可借鉴 `kevinbism/liquid-glass-effect`(MIT)+ `AkiraVoid` Acrylic/Mica 纯 CSS 分层配方(CSS 数值不涉版权,自有重写)。**新建 `assets/skins/acrylic.css`**(薄皮 uses-base):深邃底 `#0a0e1d` + 三团 accent 光晕(蓝#5b9dff/紫#b98bff/青#5fe6f0,近似 Mica 透色)+ 把 `.card/.kpi/.vs__pane/.sum-card/.bignum/.tl__card/.gantt/.diff__col/.mind__node` 升级成真毛玻璃(`backdrop-filter:blur(26px) saturate(175%)` + 半透 `rgba` 底 + inset 顶部高光 + 大柔投影 + SVG feTurbulence 颗粒 `::after` + 封面 Fluent 渐变描字)。**关键坑(全规避)**:① 禁用 `opacity` 调透明(会让 backdrop-filter 失效)→用 rgba;② `.slide{isolation:isolate}` 防多页串味采样;③ 颗粒 data-uri `#`→`%23`;④ `print-color-adjust:exact` 保 PDF;⑤ `@supports not (backdrop-filter)` 兜底退化半透实色。**接进 Studio 换皮下拉**:`build-studio.mjs` 三处清单(SKIN_ORDER/SKIN_LABEL'亚克力毛玻璃'/SKIN_DARK)各加 acrylic;**展厅** `build-showcases.py`(NEW/DARK/NAME)加;验证脚本 `verify-skins.mjs`(SKINS/SHOOT)+ 计数断言 `verify-showcases`(21→22)/`verify-studio-skins`(22→23 项)更新。重建 showcases+studio。**验证**:skins46/showcases10/studio-skins14/studio-tabs24/typecheck0 全绿;**PDF 留底**:page.pdf(printBackground)→pdftoppm 转图,vs 对比卡是实打实磨砂玻璃,毛玻璃survives PDF;**Studio 融合实测**:导入 keynote→下拉选 acrylic→预览注入 `sm-skin[data-skin=acrylic]`(含 glass token)→真换成毛玻璃,0 错。截图 `docs/screenshots/skins/{acrylic-cover,acrylic-cards,studio-acrylic-applied}.png` + `acrylic-export.pdf`。**未提交**。
>
> **✅ 动画库画廊重设计(2026-06-27,用户「界面要专业化+增加易用性有用性」)**。用户选**深色专业文档风**(AskUserQuestion)+ 4 项有用性全做。改 `build-anim-gallery.py` 的 TEMPLATE:① **深色文档站设计系统**(近黑底 #0d1017 + 顶部蓝紫光晕 + sticky 毛玻璃工具栏 + 统一深色卡片 + 字体层级)替换原浅米底白卡工具原型感;② **一键复制写法**(每卡 `.copy` 按钮→navigator.clipboard 写 `cleanVal`(去「（说明）」)+「已复制」绿反馈+execCommand 兜底);③ **明/暗底预览切换**(`#stageTog`→`body.dark-stage` 翻转 DEMO 令牌 `--paper/--ink`,浅色舞台整体转暗,让 Canvas/毛玻璃/文字效果都能在合适底色看;canvas 卡自带暗底不受影响);④ **分类导航 scrollspy**(IntersectionObserver 高亮当前类)+ **筛选 chips**(全部/新增/A–J,统一 `applyFilter` 串搜索+筛选+空状态提示);⑤ **组合预览面板**(`#combo`:进入+强调+持续三 select 从 REG 派生,叠加在同一元素实时重放+合并写法+复制)。重建画廊+studio(picker 子窗口自动继承新设计)。**验证**:anim-gallery16/studio-anim-picker15/studio-tabs24/studio-skins14/skins46/typecheck0(注:批量并发跑 playwright 时 studio-tabs 偶现 21/24=资源争用 flaky,单跑 24/24)。功能实测:filters12/jump10/combo present/copy74+已复制/新增筛选只显 _new/暗底 toggle 真翻。截图 `docs/screenshots/anim/{08-gallery-dark-top,09-gallery-darkstage}.png`。**未提交**。
>
> **✅ Studio 界面文案专业化(2026-06-27,用户报「太不专业:括号口语解释 + emoji」)**。用户选**「专业克制」**风格(AskUserQuestion 三选一):去全部彩色 emoji · 主操作按钮纯文字 · 仅工具栏纯图标按钮留中性符号(↶↷☰＋－↑↓) · 删口语化括号解释(必要说明移 `title` 悬停提示) · 语气书面化。改的全在 `packages/studio/src/main.ts`(HTML 模板 + JS 动态文案两处都要改,**坑**:`#aiCountHint`/`renderAiQueue`/`aiSentBanner`/`updateSendButton`/`applyStudioTheme`/`renderConnectState` 等运行时 `textContent`/`innerHTML` 会覆盖模板初值,光改模板没用)。代表:🎬打开动画库(所见即所得,挑一个套用)→打开动画库 · ✨让 AI 改→交给 AI 修改 · 💾保存 HTML→保存 · 🔌连接 Claude→连接 Claude Code · 🌙→主题钮改 `◐` 固定+JS 只改 title · 对齐 ⬅↔➡→左/中/右 · 强调色 accent→强调色 · 「小白也行」连接面板整段重写。保留中性符号:✕(关闭)/✥◢(画布拖拽手柄,实为 UI 上真符号)/●(状态点)/✓(已改标记)。`build-anim-gallery.py` 的 picker 横幅也去 🎬(从 Studio 打开)。重建 gallery+studio。回归 editor22/studio-tabs24/anim-picker15/newfx-studio8/select-deselect9/resilience23/save13/anim-gallery16/typecheck0 全绿(无脚本断言具体文案)。截图 `docs/screenshots/ui/{01-format,02-anim,03-ai}.png`。**未提交**。
>
> **✅ 借鉴 transitions.dev → 动效令牌 + 3 个 slide 原生新效果(2026-06-27,用户给 `https://github.com/Jakubantalik/transitions.dev`)**。该仓=21 个**可移植 CSS 微交互**(已打成 agent skill:SKILL.md+21 片段+_root.css 动效令牌+build/extract.mjs)。**两个关键判断**:① **无 LICENSE**(默认保留所有权利)→ 原样拷进我们发布的插件有再分发风险;② **大多是 App 界面交互**(弹窗/下拉/工具提示/标签/手风琴/badge/骨架/表单抖动),放映态用不上,只有少数对 slides 有用,且要从它的 hover/on-update 触发**改写成我们的放映态(翻页/分步)触发**。用户拍板=**①重写有用的几个(不 fork) ②吸收幻灯片相关子集+动效令牌**。**已做**:`_fx.css` 加 **动效令牌刻度**(`--smfx-t-quick/base/slow`、`--smfx-stagger`、`--smfx-blur-in`、`--smfx-ease-pop`,注明灵感来自 transitions.dev、自有实现)+ 3 个新效果(全填到原"规划中"槽):**A5 数字弹入** `data-anim="num-pop"`(逐字错落+模糊"砸"入;`_fx.js` 的 `splitNumPop` 自动把数字拆成 `.smfx-ch` 带 `--i`,作者也可手写预拆)、**A12 多行浮现** `data-anim="texts-reveal"`(堆叠多行依次模糊上浮,纯 CSS)、**H8 成功对勾** `class="smfx-check"`(圆环弹入+对勾描线,复用 smfx-dash)。`references/animations.md` 三个登记+令牌说明+credit;`build-anim-gallery.py` 加 3 卡(numpop 预拆 spans/texts 多行/check SVG)。**Studio 同步**:`main.ts` 的 FX_CSS(`.sm-play` 自有层,**也是 Studio 导出 deck 携带的引擎**)镜像三效果 + FX_JS `play()` 加 `splitNum`(预览+导出 deck 放映都逐字);picker 自动继承(gallery 内联),应用走 applyPicked。重建 gallery+studio。**验证**:`scripts/verify-anim-gallery.mjs` 16/16(含新卡)、`scripts/verify-newfx-studio.mjs` **8/8**(num-pop 真拆成 5 字+--i+sm-play、texts/check 落属性)、回归 fx21/resilience23/save13/skins43/showcases10/studio-tabs24/editor22/anim-picker15/select-deselect9/typecheck0 全绿。截图 `docs/screenshots/anim/06-transitions-dev-fx-{mid,settled,check}.png`。**Refine 工具**(它的 live agent 改动画面板)=我们 bridge 评论环的独立再发明,只作 v2 参考、不移植。**未提交**。
>
> **✅ 选中框 bug 修复(2026-06-27,用户报)**:Studio html-edit 模式选中元素的「蓝色框」有两病——① 点空白处不消失;② 滚动时脱离元素、悬在屏上。根因:选中=`.sm-sel`(元素自带 outline,随元素滚)+ `.sm-gizmo`(**`position:fixed`** 浮层,只在 resize 时 `positionGizmo()`,**没监听 scroll**→滚动时停在原位=「脱离悬屏」)。且点空白只 early-return 不取消选中。**修**(`packages/studio/src/main.ts` `wireFullDeckEditing`):新增 `deselectHtml()` 统一取消(去 sm-sel + 清状态 + `showHtmlSel(false)` 内含 hideGizmo);点 `.slide` 背景/空白→deselect;deck 外点击(非 gizmo)→deselect;`d.defaultView` 加 **`scroll`(capture)** + resize → `positionGizmo()` 让 gizmo 跟着滚。预览是 36 页竖排滚动列表(scrollH 14880),故必须监 iframe window scroll。已重建 studio(`node scripts/build-studio.mjs`)。验证 `scripts/verify-select-deselect.mjs` **9/9**(选中显 gizmo→点背景消失→滚动 gizmo 仍贴元素[125→-134 同步移]→Esc 取消);回归 editor22/resilience23/studio-tabs24/anim-picker15/fx21/skins14/typecheck0 全绿。**未提交**。
> 一句话: **高度 AI 整合的 HTML slides 编辑器**。人做高频细活(点字/换色/动画/移动删除元素,即时零 token)、AI 经**评论**做模糊重活;像 Claude Design comment→edit 但真 HTML 自有、只发相关页、可定制。已上 GitHub: `https://github.com/exception63/Agently-Slides.git`(remote origin,main)。
> **近况(2026-06-26 一整天)**:做了 ① reveal/impress 调研 → 动画库(A-J 十类含 Canvas 特效)+ 所见即所得画廊 + Studio 子窗口选择器;② 借鉴 `../html-ppt-skill` → 14 张新薄皮(共 21)+ 概览网格 O 键 + 7 个新版式 + 换皮/版式两展厅;③ **修薄皮「页面溢出」**(CSS 注释嵌 `*/` 吞掉令牌默认块→padding 塌 0,见下);④ **dogfood 插曲**:封面加「思考的大脑」神经网络动画(走 bridge `slidesmith_apply_patch`);⑤ **3 个收尾全做完**(Canvas 接进 Studio · 7 厚皮补 P4 · 21 皮接进 Studio 换皮下拉,见下「✅ 3 个收尾」)。**全绿,已 commit+push 到 origin/main**(277d89d/5d71663/a69fc00)。详见下方各「✅ 完成」块 + `docs/RESEARCH-{reveal-impress,html-ppt-borrow}.md`、`docs/ANIMATION-LIBRARY-PLAN.md`。**下个会话=讨论 v2「一站式 AI PPT 闭环」方向(见顶部 🎯)**。

## 🎯 下个会话(用户 2026-06-27 指定):Studio↔Claude「握手式自动协作环」
**完整交接见 `_memory/NEXT-SESSION.md`(必读)。** 目标=把桥接从"手动拉"升级成"一旦建立通讯双方握手→双向自动同步、零手动拉"。**借鉴 `claude-to-im` skill**(`~/.cc-switch/skills/claude-to-im`):它用**常驻守护进程 + `@anthropic-ai/claude-agent-sdk` 的 `query()`**(`resume` 续接 + `canUseTool` 权限网关 + 流式)做到 IM 消息自动喂给 agent、不用手动拉。用户要的流程:① 从 Claude 主动 `/slidesmith`→直接桥接好→Studio 发需求后**本 session 自动轮询到并主动改**,且 Studio 消息**在 session 内可见**;② 从 Studio 冷启动发请求→session 里冒明确提示→AI 经 slidesmith 拉取+建桥+开轮询。架构两条路(A 守护拥有 Agent / B 交互 session 自动轮询,**用户描述指向 B**,可 B 为主借 A 的握手/续接/权限),先讲取舍再做。起点=`packages/bridge/{bridge.ts,mcp.ts}`(现 pull 模型、单内存队列、无 session 归属)。
> 此前的 v2 总目标(用户 2026-06-26):"一站式 AI PPT 制作、修改、呈现闭环"。三段诊断:制作=已补选皮入口·修改=本任务打通协作·呈现(演讲者视图)+讲稿同步=再下阶段。

## ✅ 3 个收尾全部完成(2026-06-26,本会话)— 已提交+推送
三项全做完、全验证、**已 commit + push 到 origin/main**(commits 277d89d 动画库 · 5d71663 皮+版式 · a69fc00 Studio canvas+换皮)。全套 verify 绿:skins43 · showcases10 · anim-gallery16 · anim-picker15 · **studio-skins14(新)** · studio-tabs24 · editor22 · fx21(+canvas 3 项) · resilience23 · save13 · typecheck0。(注:repo 根有个 prior-session 遗留的 `anim-gallery-top.png` 未跟踪,无害,可删可留。)
1. ✅ **Canvas 引擎接进 Studio 预览/导出**。`build-studio.mjs` 把 `_fx-canvas.js` 读成虚拟模块 `@slidesmith/fx-canvas`(+`skins.d.ts`/`fx-canvas.d.ts` 声明);`main.ts` 加 `FX_CANVAS_JS`,`assembleDeck` 在 `${FX_JS}` 后注入。**关键修了 `_fx-canvas.js` 两个真 bug**:① `initIn` 用 `root.querySelectorAll` 漏掉 root 自身→满屏 `data-fx`(挂 `.slide` 上)从不触发;抽 `fxEls()` 补上 root-self。② boot 只监 `.slide.active`,但 Studio 编辑预览是滚动态**没 .active**→canvas 永不跑;改成 **IntersectionObserver**(可见即跑),统一覆盖放映态(只 active 可见)+ 滚动/编辑态(看得见就跑)+ 更省。实测:预览里 1 个 canvas 在画(只可见页)、导出烘焙 HPX+data-fx。
2. ✅ **原生 7 厚皮补 P4 版式**。抽出 **`assets/_layouts.css`**(kpi/vs/timeline/gantt/roadmap/diff/mindmap,token-generic + 缺失令牌就地兜底:`--accent-3→--accent-2` · `--good→--green→中性绿` · `--radius→0`);`build.py` 新 `{layouts}` 槽对**所有皮恒内联**;`_components.css` 删 P4 段(薄皮改从 _layouts 拿,单一真相)。实测 editorial(浅)+keynote-dark(暗)9 张 P4 全渲染、兜底解析成真颜色、零溢出。
3. ✅ **21 张皮接进 Studio 换皮下拉**。`build-studio.mjs` 暴露 `@slidesmith/skins`=每皮一个**可注入 bundle**(薄皮=`_components+_layouts+skin`,厚皮=`_layouts+skin`)+ 名称/暗浅/字体 URL。`main.ts`:`H.skin` 状态、格式 pane 加「皮肤·换一套气质(21套)」下拉、`skinInject()` 在 `assembleDeck` 把 `<style id="sm-skin" data-skin>`+ 字体 link 叠加在 deck 自身 CSS 之后(后者被覆盖=重新着皮)、导出烘焙、再导入 `loadHtmlDeck` 识别 data-skin 并 strip 去重、undo Snap 加 skin。实测:cartesian→vaporwave 令牌真变(accent #8A8178→#ff6ec7,paper→#1a0938)、导出→再导入还原下拉且恰 1 个 sm-skin、切「保持原样」干净移除。**坑**:① CSS 注释别嵌 `*/`(见薄皮溢出修复);② 注入层必须排在 `${H.head}` 之后才压得过 deck 自带皮;③ Studio 单文件大小 511KB→1100KB(内嵌 21 皮 bundle)。

> 下一步候选:图表(Chart.js 内联/预渲染,破单文件要权衡)、展厅/动画库挂进 Studio 入口、换皮下拉只在 html 模式有意义(IR 模式仍走 `#theme` 3 套)。**老的 bridge 反向连接闭环**仍可用(`mcp__plugin_slidesmith_slidesmith__*` + 后台轮询 `curl /api/requests?drain=0`,超时和命中都 exit 0 要读 output 区分)。

## Current State(已完成)
- **Studio** `studio/slidesmith-studio.html`(单文件离线 355KB);源 `packages/studio/src/main.ts`,改后 `npm run build:studio`。
- **桥接** `packages/bridge`:`slidesmith serve`/`mcp`;HTTP+WS+MCP+控制 API(`/api/{status,requests,patch,open}`+CORS)。**插件**`plugin/`(市场 `slidesmith-local` + `.mcp.json` + `/slidesmith` 命令)已装。MCP 4 工具 `slidesmith_open/get_requests/apply_patch/status`。
- **插件打包出片三件套(2026-06-26,plugin v0.2.0,commit 3cc16d9)**:`editorial-slides`/`transcripts_html`/`slides-presenter-mode` 已迁入 `plugin/slidesmith/skills/`(自动发现,调用 `/slidesmith:<名>`,自然语言仍自动触发)。**唯一来源=插件**:原 `~/.claude/skills` 入口已拆(editorial 真目录→`~/.claude/_slidesmith-migrated-backup/`;另两个原是软链→AutoCourses,已删软链,AutoCourses 原件仍在但不再加载)。`auto_courses/SKILL-MAP.md` 已改引 `/slidesmith:*`(AutoCourses 从此依赖本插件)。`sim-livecase` 不收(非 slides skill)。**坑**:`claude plugin update` 把插件**按版本拷到 cache**(`~/.claude/plugins/cache/slidesmith-local/slidesmith/0.2.0/`),改了源要 `claude plugin marketplace update slidesmith-local` + `claude plugin update slidesmith@slidesmith-local`,且**重启 Claude Code 才生效**(skill 和 MCP 一样新会话才加载)。验证:重启后 `/slidesmith:editorial-slides` 等出现、裸名版消失。
- **编辑器功能**:页内评论(跟随当前页·队列·徽标 ●待发送/●已发送脉冲/✓已改)· 整份 deck 评论 · 审阅「↩︎还原本页」· 直接编辑(选中元素 ↑↓🗑)· 动态进度横幅 · 🔌连接 Claude 按钮(探测+一键跳连接版+带 deck)· 视觉自检 · **💾 保存 HTML(原地覆盖导入文件)** / 导出 HTML 副本 / 导出 PDF。
- **保存 HTML(2026-06-25,commit cda8515)**:新增「保存 HTML」=用 **File System Access API 原地覆盖**用户导入的那个 .html。导入时(open-picker `#imp` + drag)抓 `fileHandle`,`saveHtmlInPlace()` 有句柄→静默覆盖;无句柄(桥接来的 deck/首存)→`showSaveFilePicker` 弹一次记住;无 API(Safari/FF)→下载副本。**桥接不行**(deck 经 `POST /api/open` 交接只剩 basename,无磁盘路径),故走浏览器。移除旧 **存 .json/存 .md**(HTML-first 后在 html 模式只弹提示无实效)+ 删 `irToMarkdown` import。钩子 `__SM_SAVE_HTML__`/`__SM_HAS_FILE_HANDLE__`。验证 `scripts/verify-save.mjs` 13/13。**连不上 Claude 的原因**:桥接 MCP 插件只在 Claude Code **新会话启动**才连(不是 bug)。
- **验证全绿**:`scripts/verify-{bridge(14),editor(22),connect(7),save(13)}.mjs` + `dogfood.mjs`;40 单测;typecheck 0 错。注:`verify-{connect,bridge}` 需 `npx tsx`(引 bridge 的 `.js`→`.ts`),其余 `node` 即可。截图 `docs/screenshots/{bridge,editor,dogfood,live,save}/`。
- **文档**:README/GUIDE/AGENTS 已清洗成纯现状(无历史);`packages/bridge/README.md`、`plugin/slidesmith/README.md`、`docs/DECK-CONTRACT.md`。

## Next Likely Action (v2)
- ✅ **N1 完成(2026-06-24)**: `docs/DECK-CONTRACT.md` 写好(L1/L2/L3 合规层级);Studio 加"导入 HTML"(`mode:'ir'|'html'`,DOMParser 解析 `#deck>.slide` 出 title/seg/segname/variant,srcdoc 自渲染,左栏列 36 页,点页→驱动 deck 原生 thumb.click 导航,HTML 模式 banner,导出原样 HTML,`window.__SM_IMPORT__` 自动化钩子)。实测拖入 keynote.html=36 页+6 段+36 缩略图+0 报错。截图 `docs/screenshots/n1/`。验证脚本 `scripts/verify-n1.mjs`(playwright-core file://)。
- ✅ **N2 完成(2026-06-24)**: 就地编辑 v1。html 模式改为**单页干净编辑面**(deck head 样式 + 自适应 fit,不跑 deck 引擎);因 srcdoc 同源,**parent 直接操作 iframe DOM**(无需节点寻址):isTextLeaf 元素挂 contentEditable;点选元素→检查器调 字号/颜色/粗细/对齐(inline style)+ data-anim 动效;换肤=改 `--accent/--paper/--ink` 令牌 + data-theme 主题切换。导出=按 prelude+`#deck`{各页 outerHTML}+trailing 重组(harvest 当前页 outerHTML,剥 contentEditable/sm-sel)。**令牌覆盖走 `<html>` inline style(高优先级,压过 `:root[data-theme]`)**,导入时再解析回 H.overrides。`__SM_EXPORT_HTML__` 自动化钩子。实测**全回路**:改字+换 accent→导出→重新导入,改动全在(36页0错)。截图 `docs/screenshots/n2/`,验证 `scripts/verify-n2.mjs`。
- ✅ **N2 修订(用户反馈)**: 编辑面改回**渲染完整 deck(含自带引擎)**→修复"变正方形"(单页 flex 压缩所致)+ 找回 deck 自带缩略图段导航;Studio 左侧页列表可折叠(☰),html 模式自动折叠(用 deck 自带导航)。编辑/换色/导出回路实测仍全过(aspect 1.78 · segs 6 · thumbs 36)。
- ✅ **N3 完成(2026-06-24)**: Submit-to-AI 回路。导入时给每页注入稳定 `data-id`(SLIDE_MAP/自增)。检查器加"Submit to AI"区:目标页(选中元素/active 页)+ 指令框 → `buildAiRequest()` 导出自包含 `<deck>.<id>.ai-request.md`(指令+该页当前HTML+设计令牌+契约提示)→ AI 只重写那页 `<section data-id>` → `applyAiPatch()` 按 data-id 精确替换(单段无id则落当前页),harvest 别页后 re-render。钩子 `__SM_AI_REQUEST__`/`__SM_APPLY_PATCH__`。AGENTS.md §6b 写了 AI 该怎么处理请求。实测:改第5页→只它变(别页/封面不动)、export→re-import 仍在(36页0错)。截图 `docs/screenshots/n3/` + 样例请求文件;验证 `scripts/verify-n3.mjs`。小瑕疵:应用补丁后 deck reload 跳回第1页(目标页标签随之回到第1页)——N4 可顺手保位。
- ✅ **N4 完成(2026-06-24)**: 交付套件接到导入 deck。① **视觉自检进 Studio**(M8 给人用): `auditImportedDeck()` 在浏览器量每页渲染后布局(scrollHeight 溢出 / WCAG 对比度 / 坏图,transform-immune),检查器"视觉自检"按钮出可点报告(点条目跳页);② **PDF 导出**(纯浏览器 print): `pdfPrintHtml()` 注入 print CSS(每页一张 1920×1080)+ window.open 打印 → 另存 PDF(实测 36 页);③ **修复 N3 保位**(`htmlGotoAfterRender`,应用补丁后停在那页不跳首页)。钩子 `__SM_AUDIT__`/`__SM_PDF_HTML__`。实测:净 keynote 0 问题、注入溢出被抓(第4页 1101px)、PDF 36 页、保位 idx=3。截图 `docs/screenshots/n4/` + exported-deck.pdf;验证 `scripts/verify-n4.mjs`。**演讲者/讲稿同步留作后续**(需从契约 deck 抽 notes,较 fuzzy;keynote 自带演讲者钮另说)。
- ✅ **聚焦 + 打磨(2026-06-25)**: 项目收窄为"快速改 slides 编辑器"(讲稿同步/双屏交给其它 skill,不做)。① 加回**持续动效**(data-motion glow/breathe/float/pulse/neon/stress,MOTION_CSS 注入预览+导出);② Submit-to-AI **批量化**(每页指令记住可改 + 「导出全部」一次性 + 粘贴/文件应用);③ **修真 bug**: 就地编辑的 contentEditable+点选 wiring 之前挂在 iframe load(被外部字体卡住)→静默失效,改成轮询 `#deck .slide` 出现即 wiring。验证 `scripts/verify-polish.mjs`(全过)。
- ✅ **桥接最小闭环完成(2026-06-25)**: 新包 `packages/bridge`(`@slidesmith/bridge`)=本地中间人,消除 Submit-to-AI 手动搬文件。`startBridge()` 用 node:http 服 Studio + `ws` WebSocket + 内存持 deck+请求队列;`packages/bridge/src/mcp.ts` 用 MCP SDK(stdio)包出 4 工具 `slidesmith_open/get_requests/apply_patch/status`(进程内嵌 HTTP+WS)。**Studio 连接模式**(main.ts:`connectBridge`/`submitRequests`):http 源→连 `ws://location.host`→顶栏绿「● 已连接 Claude」+ 「🚀 发送给 Claude」走 WS;`file://` 无 host→静默退回手动。CLI 加 `serve`(给人,开浏览器)/`mcp`(给 Claude Code)。钩子 `__SM_BRIDGE__`/`__SM_SEND_ALL__`。**全闭环 headless 实测**:开 deck→Studio 连上+收 deck(36页)→写意见点发送→bridge 收到→get_requests 拿到(含 data-id+指令)→造补丁 apply_patch→Studio 当场改对那页→导出仍在;MCP stdio 列出 4 工具。`scripts/verify-bridge.mjs`(tsx)14/14 过,截图 `docs/screenshots/bridge/`。接口文档 AGENTS.md §4b + `packages/bridge/README.md`。
- ✅ **Claude Code 插件 + 部署 + dogfood 完成(2026-06-25)**: ① **插件** `plugin/`(本地 marketplace `slidesmith-local` → 插件 `slidesmith`):MCP 走**单独 `.mcp.json`**(扁平 name→config,非 `mcpServers` 包裹,放插件根)+ `commands/slidesmith.md` 命令。MCP 启动命令用**绝对路径** `node + node_modules/tsx/dist/cli.mjs + packages/cli/src/index.ts mcp`(因装插件会**拷贝到 cache**,`${CLAUDE_PLUGIN_ROOT}` 不可靠;绝对路径指向仓库才稳)。② **部署**(headless,用 `claude` CLI v2.1.162):`claude plugin marketplace add <repo>/plugin --scope user` + `claude plugin install slidesmith@slidesmith-local --scope user`;`details` 显示 MCP servers(1) slidesmith;已写进 `~/.claude/settings.json` 的 `extraKnownMarketplaces`+`enabledPlugins`。**坑**:新 MCP 仅**下次启动**才连(本会话内 ToolSearch 搜不到 slidesmith 工具)。③ **bridge 加 HTTP 控制 API**(`/api/status`·`/api/requests` GET 取走·`/api/patch` POST)= 不靠 MCP 也能驱动 serve(curl/dogfood 用)。④ **patch 后 Studio 回灌 deck**(`syncExportToBridge` 发 `exported`,bridge 更新内存 deck,晚到的 studio 也见改动)。⑤ **EADDRINUSE 回退**:8765 被占→自动换空闲端口(避免遗留 serve 与插件 MCP 抢端口)。⑥ **全链路 dogfood 通过**:`slidesmith serve keynote` 真浏览器弹出+已连→headless「用户」写意见点发送→`GET /api/requests` 拿到→改写那页标题→`POST /api/patch`→Studio 当场改对(第3页标题→「桥接闭环·DOGFOOD✅」,其它不动)+晚到者也见(回灌)。`scripts/dogfood.mjs`,截图 `docs/screenshots/dogfood/`。
- ✅ **产品重定位 + 编辑器重构 ①-④ 完成(2026-06-25)**: 用户拍板 = **高度 AI 整合的 HTML slides 编辑器**;核心原则**分工**(人做高频细活:点字/色/字号/动画/移动/删除元素=即时零 token;AI 做模糊重活=经评论)。像 Claude Design 的 comment→edit,但**真 HTML 自有+只发相关页省 token+全可定制**。给用户看了 mockup(show_widget)对齐方向:AI 角色=**只按我的评论改**(不主动提建议);构建顺序 ①→④ 全做。**① 页内评论闭环**:评论**跟随当前页**(修了原 bug——`navSyncTimer` 轮询 deck `.active`+`updateAiTarget` 改用 `cur`;左列 html 模式不再折叠,每页带徽标 ●待发送/✓已改;右栏「全部任务」队列可点跳;`aiApplied` Set 记已改;apply 标✓不删评论)。**② deck 级评论**:「对整份 deck 说」→ 一个全局任务,请求带**结构总览**(36 页 № · data-id · 标题),一次性随页任务发。**③ 审阅/还原**:`aiBefore` 存改前 HTML,applied 页出「↩︎ 还原本页」→ 回到 AI 改前(评论留存→回到待发送)。**④ 直接编辑增强**:html inspector 加选中元素 ↑↓🗑(直接改 iframe DOM+harvest 持久)。`scripts/verify-editor.mjs` 22/22 过,截图 `docs/screenshots/editor/`。AGENTS.md §4b 更新请求新形态。**插件 MCP 本会话已连**(`mcp__plugin_slidesmith_slidesmith__*` 可用)。
- ✅ **真实 live dogfood + 进度提示 + 上 GitHub(2026-06-25)**: 用原生 MCP(`mcp__plugin_slidesmith_slidesmith__*`)真跑用户的多轮编辑:红字+呼吸灯 / 触发动画(直线 draw 成缠绕辫子,用页内 scoped `<style>`+`.slide.active[data-id]` 触发)/ 删封面 meta 项。**自动接活回路**:后台 `curl /api/requests?drain=0` 轮询监听用户「发送」→ 完成即通知我 → 我 get_requests+apply(注意:超时和命中都 exit 0,要读 output 区分)。**进度提示(用户要带动效)**:新增 `aiSent` Set;徽标 3 态 待发送●橙 / **已发送●蓝脉冲** / 已改✓绿;队列同步;蓝色提醒横幅 `#aiSentBanner` 闪烁点「已发送 N 个任务,Claude 正在改…」;状态机 send→sent / apply→done / edit·revert→回退。`docs/screenshots/editor/03-progress-sent.png`,verify 8 项全过。**保存**:serve 的 deck 只在内存,加了「存盘」=headless 连 bridge `__SM_EXPORT_HTML__` 写 `dist/keynote-edited.html`(用户改动只在 bridge 内存+其浏览器 tab,易丢)。**已 git init + push 到 `https://github.com/exception63/Agently-Slides.git`**(commit 6ec69c6,main,172 文件,node_modules/dist 已忽略,osxkeychain 凭据)。
- ✅ **Studio Keynote 化重构 + 流光/消失动画 + 字体库 + 嵌入字体(2026-06-26)**: 右栏 html 检查器从平铺改成 **Keynote 三 tab**:`格式 / 动画效果 / AI 修改`(`.htab`+`.hpane[data-hpane]`,与 IR 模式旧 `.tabs/[data-pane]` 并存互不影响,setHtmlMode 只切旧的)。**格式**:主题配色 + 选中元素「字体下拉(optgroup 离线/英文/中文)·字号·颜色·**B/I/U 切换钮**·**L/C/R 对齐钮**·粗细精细·↑↓🗑」。**动画效果**:子 tab `进入(data-anim)/动作(data-motion)/消失(data-anim-out)` + 触发方式(auto/manual)+ 预览钮。**AI 修改**:原 Submit-to-AI 全家(目标/指令/队列/发送)+ 视觉自检搬这。**新动效**:① `shimmer 流光溢彩`(镂空文字彩虹液态,加进 `MOTION_NAMES`+FX_CSS+runtime/core.css);② **消失动画**(`ANIM_OUT_NAMES`=fade-out/sink/zoom-out/out-left/out-right):因引擎 `.slide{display:none}` 瞬切会切断退场,FX_JS **capture 阶段拦截键盘(e.key,引擎读 e.key)/`[data-act]` 按钮** → 先播退场(440ms,leaving 页仍 active 可见)→ 带 `bypass` 旗标 re-dispatch 让引擎真翻页;编辑态 `data-smfx-edit="1"` **关拦截保流畅**,Studio 用 `__SM_FX_PLAY_OUT__` 原地预览。**字体库**:`FONTS[]`(系统离线 3 + Google 英文 5 + 中文 6:Inter/Space Grotesk/Montserrat/Playfair/JetBrains/Noto Sans·Serif SC/站酷小薇·快乐/马善政/霞鹜文楷);选字体写 `style.fontFamily` + `ensureFontLoaded` 往预览注 `<link>`;`fontLinksFor()` 在 assembleDeck 注入(deck 已自带 google link 该字体则跳过)。**嵌入字体(导出选项,header 复选框)**:`buildExportHtml()`→`embedFonts()` 抓 deck 实际用字(`deckChars`)→ Google CSS2 `&text=` **子集化**(中文 4 字仅 1.5KB)→ fetch woff2 base64 内联成 `@font-face data:`,删除远端 `<link>/@import` → **完全离线自包含**(实测 CORS `*` 通)。`saveHtmlInPlace`/`#expHtml`/`#saveHtml` 均走 async buildExportHtml。钩子 `__SM_BUILD_EXPORT__`。验证 `scripts/verify-studio-tabs.mjs` **23/23**(含真嵌入:woff2 inlined+remote link removed);回归 verify-{editor(改:AI/格式控件进了 tab,测试先点对应 htab)22,fx18,save13,polish,n1-4} 全过。注:`verify-{bridge,connect}` 失败是**既有**问题(缺编译 `bridge.js`,与本次无关)。**改的文件**:`packages/ir/src/tokens.ts`、`packages/studio/src/main.ts`(主体)、`packages/runtime/src/core.css`。**需 `node scripts/build-studio.mjs` 重建** `studio/slidesmith-studio.html`(bridge 每请求重读,刷新浏览器即见)。

## 🗺 优化路线图(2026-06-26 起,逐步做)
**见 `_memory/optimization-roadmap.md`(逐项勾选)。** 四视角评审后定的优先级清单。
- ✅ **第一步 = top-3 完成(2026-06-26)**:① 防丢失(localStorage 自动存草稿+「●未保存」红点+beforeunload+恢复横幅+Cmd/Ctrl+S)② 撤销/重做(快照栈,700ms 合并,Cmd+Z/Shift+Z+↶↷ 钮,parent+iframe 都转发)③ HTML 模式插入图片(选文件/粘贴→base64 内联,入 undo)。
- ✅ **直接操作 + 先做批次完成(2026-06-26)**:④ 画布拖拽 gizmo(✥ 移动 / ◢ 改大小,移动=transform translate、改大小=width,坐标按 deckScale 换算,gizmo position:fixed 挂 iframe body 不被 harvest)+ 检查器宽度/复位 + 方向键微调;⑤ 快捷键(翻页/微调/Esc/Delete,带输入框守卫);⑥ Studio 暗色模式(🌙 切换+localStorage 持久);⑦ 嵌入字体并行(Promise.all+fetchTimeout 15s+setBusy 进度);⑧ 修复 CI(verify-bridge/connect 自重启到 tsx,`.ts` import 改 dynamic)。**全套 12/12 通过**,`scripts/verify-resilience.mjs` 23/23。代码全在 `packages/studio/src/main.ts`(+ 两个 verify 脚本)。
- **下一批(未做,排期)**:harvest/单一数据真相重构、main.ts 拆模块、向上选一层、AI 端可见性;顺手:emoji→线性图标、SMWindow 类型集中、aria。详见 `_memory/optimization-roadmap.md`。
- ✅ **排版 text-wrap(2026-06-26)**:标题 balance + 正文 pretty,Studio 注入 `TYPO_CSS` + core.css 两处。评估 `@chenglou/pretext` 后**暂不接入**(HTML-first 用不上其无浏览器测量,server-side 未出,v0.0.8 太早;触发回看点见 roadmap)。

## 🎯 reveal/impress 调研 + 动画库(2026-06-26) — ✅ 大部完成
**调研结论 = C(重心 B)**:把 reveal 的神奇移动/分步揭示/转场移植进引擎(单文件/离线/可编辑/AI可写),impress 只取缩放概览创意 + Prezi 逃生舱。详 `docs/RESEARCH-reveal-impress.md`。用户拍板"按推荐做"。
**接着用户要"集众家所长的风格库式动画库"**:广泛调研(Animate.css/Animista/Magic · GSAP/Motion/Anime + **View Transitions API** · reveal/Slidev/Keynote/PPT · Rough Notation/confetti/SVG line-draw)→ 方案 `docs/ANIMATION-LIBRARY-PLAN.md`(9 类 ~60 编号 · 全标出处+实现 · 分核心/点睛/进阶层)。**最大发现:View Transitions API**(2025-10 Baseline,同文档版)让"神奇移动"几乎零 JS,`data-id→view-transition-name`,FLIP 兜底。
**已建成(editorial-slides 这个 plugin skill 里)**:
- **动画库引擎** `assets/_fx.css`(全 keyframes/类) + `_fx.js`(暴露 `window.SMFX`:onActive 入场/强调重播 · transition 神奇移动 View Transitions+FLIP · stepForward/Back 分步 · 点睛 init)。触发模型:页/台上 `.smfx-arm`(藏)/`.smfx-go`(播)/`.smfx-exit`(消失)。
- **引擎钩子**:`_engine.js` 的 `setActive` 包了 `SMFX.transition`+`onActive`,`next/prev` 先问 `SMFX.stepForward/Back`(全 **gated 到 present 态**,编辑/滚动态不藏内容;SMFX 缺失则 no-op)。`build.py` 自动内联 `_fx.css`(skin 后)+`_fx.js`(engine 前)。
- **所见即所得画廊** `gallery/animations.html`(单文件):9 类 · 57 卡 · 每卡 编号+实时预览+重播/下一步/播放+`属性写法`,顶部搜索+A–I 跳转。由 `assets/build-anim-gallery.py` 生成(内联 _fx.css/_fx.js,REG 数据驱动)。**像剪映效果面板**。
- **AI 映射** `references/animations.md`(编号→属性:入场`data-anim`/强调`data-emph`/持续`data-motion`/消失`data-anim-out`/分步`class=fragment`/转场`data-transition`/神奇移动两页同`data-morph`)。SKILL.md 加 **Step 0.5 选动画** + description 含动画触发词 + 版本 1.4.0。7 张皮 gallery 已重建(都带 FX)。
- **验证** `scripts/verify-anim-gallery.mjs` **12/12 过**。截图 `docs/screenshots/anim/{01-top,02-full,03-magic-move}.png`。本机 Chromium 支持原生 View Transitions。
**坑记**:① playwright headless 派发键盘事件要在 **`document.body`** 上(engine handler 读 `e.target.matches`,document 无此法会抛);② MCP playwright **禁 file://**,起 http.server 或用 playwright-core node 脚本。
**已实现编号**:A1-4/6-11 · B1-7 · C1-7 · D1-7 · E2/4-9 · F1/3/4 · G1-9 · H1/5/6/7 · I1/2。**规划中**:A5/A12 · E1/3/10/拼块 · F2 · H2/3/4/8-11(H4 RoughNotation/H11 confetti 零依赖) · I3/4。
**① 已做(2026-06-26):动画库接进 Studio = 子窗口选择器**。用户拍板"用子窗口接画廊"。做法:`build-studio.mjs` 把 `gallery/animations.html` 内联成虚拟模块 `@slidesmith/anim-gallery`(+ `packages/studio/src/anim-gallery.d.ts` 声明);Studio「动画效果」tab 加红钮 **🎬 打开动画库** → `window.open` 一个 **Blob URL + #picker** 子窗口(就是那个画廊,picker 模式每卡多「应用到选中」)→ 卡片 `postMessage({type:'smfx-pick',spec})` 给 `opener` → Studio `applyPicked()` 落到 `htmlSelEl`(attr: data-anim/emph/motion/anim-out/transition;class: fragment/smfx-*;morph: 两次配对 pendingMorphId)→ `showHtmlSel` 渲染「当前动画」**chips**(✕ 移除)+ `previewPlayFx` 当场预览。画廊 picker 检测 `location.hash` 含 picker 或 `window.opener`。**Studio FX_CSS 扩了**新入场(tracking-in/focus-in/slide-blur/flip-in/back-in)+ 强调(data-emph 七个)+ 点睛(kenburns/draw/clip-wipe/spot),preview 能播。`packages/studio/src/main.ts`(import + openAnimGallery/applyPicked/applyMorph/renderAnimChips + FX_CSS + anim pane UI + CSS)。**`node scripts/build-studio.mjs` 重建**(455KB,内嵌画廊)。验证 `scripts/verify-studio-anim-picker.mjs` **15/15**(画廊回传 + Studio 选中→应用→chips→移除→神奇移动配对 + 0 错);回归 studio-tabs24/editor22/fx18/resilience23/anim-gallery12 全过 · typecheck 0。截图 `docs/screenshots/anim/04-studio-picker.png`。**坑**:Studio 是单文件,够不到磁盘上的画廊 → 必须 build 时内嵌;Blob 子窗口能 postMessage 给 opener(同源)。
**下一步候选**:② 补规划中的明星(F2/H4 批注/H11 撒花/逐字浮现 split-reveal/cube/百叶窗);③ 用户在真 deck 里点名几个动画 dogfood;④ 弹窗被拦的 in-app 抽屉兜底(现仅提示);⑤ 转场/分步在 Studio 单页预览的保真(现属性照落、导出/放映生效)。

## 🎬 借鉴 html-ppt-skill → Canvas 特效层(2026-06-26) — ✅ P0 完成
用户给参考项目 `../html-ppt-skill`(模板/动画/换皮/showcase 都牛),要"集众家所长增强"。3 路 agent 编目 → 方案 `docs/RESEARCH-html-ppt-borrow.md`。**它的架构**:thin token 主题(36 皮,`base.css` 共享组件层)+ 两层动画(`animations.css` CSS keyframes + **`fx/*.js` 20 个零依赖 canvas 特效**,读主题色,`[data-fx]`+MutationObserver 生命周期)+ runtime(概览网格 O / 演讲者弹窗 S)+ 31 版式 + showcase(iframe 隔离同款 demo + copy-chip + scale(.5) 活缩略)。
**借鉴优先级**:P0 Canvas 特效(最缺最炫)· P1 多换皮(~13 新气质,token 近兼容)· P2 主题/版式 showcase · P3 概览网格 O 键 · P4 版式库(KPI/对比/时间线/甘特/路线图/架构/思维导图)。**不取**:演讲者双屏深做 · Chart.js/highlight.js CDN(破单文件) · sparkle-trail/word-cascade。
**✅ P0 已建成**:移植 13 个 canvas 模块 → `assets/_fx-canvas.js`(harness `_util` 改读我们 `--accent/--accent-2/--ink`;canvas `insertBefore` 作背景垫底;自带 MutationObserver 监 `.slide.active` init/stop)。动画库新增**第 10 类「J · Canvas 特效」**:J1 极光团 gradient-blob · J2 星座 constellation · J3 知识图谱 knowledge-graph · J4 神经网络 neural-net · J5 数字爆炸 counter-explosion · J6 粒子迸发 · J7 冲击波 · J8 星空 · J9 星系漩涡 · J10 矩阵雨 · J11 数据流 · J12 礼花 · J13 多行打字机。用法 `data-fx="名"`(满屏加 `.slide` 上 / 局部加盒子上)。`build.py` 内联 `_fx-canvas.js`(新 `{fx_canvas}` 槽);`build-anim-gallery.py` 加 J 类(canvas 卡 + IntersectionObserver 只跑可见的 + Replay)+ pickSpec canvas case;`references/animations.md` 加 J 表。**Studio**:rebuild 后 picker 子窗口直接展示 J 类**活 canvas**,应用落 `data-fx`(scope slide)+ 专属提示(canvas 在生成/导出 deck 放映时跑,Studio 预览暂不渲染)。验证 `verify-anim-gallery.mjs` **16/16**(J 类 13 卡 + 12 活 `<canvas>` 跑起来);picker15/tabs24/typecheck0 全过。截图 `docs/screenshots/anim/05-canvas-fx.png`(13 特效全渲染:星系/冲击波/知识图谱/2,386 数字爆/矩阵雨…)。**坑**:canvas 自带暗底,浅皮上会把区域压暗(也算用法);Studio 预览/导出跑 canvas = 待做(iframe srcdoc 重渲染会换文档,无跨渲染泄漏,可后续注入 _fx-canvas 进 buildExportHtml)。
**✅ P1-P4 全做完(2026-06-26,用户"P1到P4全部做了")**：
- **P1 多换皮(7→21)**：关键架构发现——原 7 皮是**厚皮**(每皮自带全套组件 CSS)，不像 html-ppt 的薄皮+共享 base。故抽出 **`assets/_components.css`**(token-generic 组件层 + P4 版式)，新薄皮只写 `:root` 令牌块 + `/* uses-base */`(build.py 据此内联 _components.css；原 7 厚皮无此标记、不内联、零回归)。新增 **14 薄皮**：dracula/nord/tokyo-night/catppuccin-mocha+latte/vaporwave/swiss-grid/bauhaus/cyberpunk-neon/glassmorphism/y2k-chrome/neo-brutalism/terminal-green/rose-pine(调色移植 html-ppt,签名微调如 vaporwave 渐变标题/swiss 12 栏网格底/bauhaus 硬阴影/neo-brutalism 粗框)。`skins/` 21 文件。
- **P3 概览网格(O 键)**：`_engine.js` 加 toggleOverview——克隆每页→`scale` 成 16:9 缩略网格→点击 setActive 跳页，Esc/点背景关。同时加 **`?bare` 模式**(只显某页+去界面，供 showcase 缩略 iframe)。deckAPI 暴露 toggleOverview。
- **P4 版式库**：`_components.css` 加 kpi-grid/vs 对比/timeline/gantt/roadmap/diff/mindmap(SVG)（纯 CSS,21 皮通用）+ 文档进 `references/components.md`。
- **P2 展厅(所见即所得)**：`build-showcases.py` 生成 ① **`gallery/theme-showcase.html`**=21 皮**活封面 iframe 网格**(`skins/<skin>.html?bare` + transform scale,点击开全屏；借 html-ppt 的 scale-live-iframe)②**`gallery/layout-showcase.html`**=版式样板 deck(按 O 看全部 9 版式)③`gallery/skins/<skin>.html` 21 单皮样板。demo 原料 `demo/_showcase.slides.txt`+`_layouts.slides.txt`。
- **验证**：`verify-skins.mjs` 15/15(14 皮构建+渲染+组件齐全+概览) · `verify-showcases.mjs` 4/4(21 iframe + 9 版式) · 回归 anim-gallery16/picker15/tabs24/typecheck0。截图 `docs/screenshots/skins/*.png`(vaporwave/neo-brutalism/theme-showcase/layout-overview…都漂亮)。方案 `docs/RESEARCH-html-ppt-borrow.md`。SKILL.md 1.5.0(21 皮+10 动画类+版式+概览)。**坑**：file:// 跨帧读 contentDocument 被拦(改判 iframe src);present 非全屏截图有淡重影(正常)。
**下一步候选**：Studio 导出注入 canvas 引擎让其真跑 · Studio 换皮下拉接这 21 皮 · 原生 7 厚皮也补 P4 版式 · 图表(Chart.js 内联/预渲染)。
**✅ 薄皮「页面溢出」修复(2026-06-26,用户报)**：用户看薄皮样张报"明显页面溢出"(KPI 卡顶到底边)。根因 = **CSS 注释不能嵌套**——`_components.css` 头部大注释块里写了字面量 `/* uses-base */`,那个内层 `*/` **提前闭合了整个注释**,把紧跟其后的 `:root{…默认令牌…}` 整条规则吞成无效选择器被浏览器丢弃 → 薄皮凡是没自定义的令牌(尤其 `--pad-x/--pad-y`、`--t-*` 字阶)全丢 → `.slide{padding:var(--pad-y) var(--pad-x)}` 因 var 为空而 invalid-at-computed-value→**padding 塌成 0**→内容贴边、`.fill` 顶到 1080 看着像溢出。**修**:把注释里的 `/* uses-base */` 改成「uses-base」纯文字(并加"本注释块严禁出现 `*/`"警示),`build.py` 检测薄皮仍读 skin 文件里那行独立完整的 `/* uses-base */`(不受影响)。重建 `gallery/skins/*`+showcases。**实测**(playwright deck 态量):root `--pad-y`=80px、slide padding=80px、6 页内容底 ≤1042 全 ok。**坑(非本 bug)**:present 放映态在 1280×760 这种非 16:9 视口下,引擎 transform 居中会把 slide 底部 padding/footer 切到视口外(slide 占 187–893 > 760)→**之前提交的 present 态截图看着仍卡底,实为视口裁切+已知重影,不是内容溢出**;故把 `verify-skins.mjs` 截图改为 **deck 态 slide-wrap 元素截图**(干净如实)。**回归守卫**:`verify-skins.mjs` 新增两断言——`--pad-y` 必须非空(注释 bug 的 canary)+ 每页内容底 ≤1084(通用溢出哨)——**43/43 过**(原 15);showcases4/anim-gallery16/picker15/typecheck0 全绿。**未提交**。

---
（原始任务说明留档：）
**调研 reveal.js + impress.js,看能给 Slidesmith 加什么 —— 核心是 AI-first。**
- 用户的真实问题:让 **AI 帮做"高端炫酷"的 HTML slides** 时,怎么把这两个项目用上?
- **关键决策**(用户要我权衡给方案):是 **(A) 直接部署 reveal/impress,需要时让 AI 调用**,还是 **(B) 以某种方式整合进本项目**(借鉴/移植其能力到 Slidesmith 引擎),还是 **(C) 混合**。
- 调研要点:reveal.js(Markdown→deck、片段动画 fragments、auto-animate 形变过渡、speaker notes、PDF 导出、plugins/主题、纵向 slides)· impress.js(无限画布 + 3D 平移/旋转/缩放转场,"prezi 式")。
- 对齐本项目约束再判断:Slidesmith = **单文件、离线、可移植、固定 1920×1080、契约 HTML deck、HTML-first、人改细活/AI 改重活**。哪些能力**值得**吸收(如 auto-animate 形变、fragments 分步、impress 的画布缩放转场),哪些与"单文件可移植"冲突(如 reveal 的多文件/插件生态)。
- 产出:给用户一个**带优先级的方案**(A/B/C 哪个、各能力怎么落地、AI 在其中怎么"被调用/被整合")。先调研再动手,别直接改代码。
- 起点:`_memory/active.md` + `_memory/optimization-roadmap.md` + `docs/PIVOT-v2-html-first.md` + `AGENTS.md`(看现有引擎/契约边界)。

## ⭐ 下一步(2026-06-25,上下文将清空)
**完整计划见 `_memory/NEXT-SESSION.md`(必读)。** 插件+闭环+dogfood+编辑器重构①-④ 已成。剩**收尾**:
- **重启后用原生 MCP 跑一遍**:`/slidesmith <deck>` → 我用 `mcp__slidesmith__slidesmith_open` 等原生工具走一轮(本会话只验到 MCP server 起+列工具+control-API 闭环,**没经 Claude Code 自带 MCP 客户端实跑** slidesmith_open)。这是唯一没现场验的链路。
- 可选打磨:Studio「人类/AI 协作」两套模式键位更清晰分离;serve 的 deck 存盘/`slidesmith_save`;把插件发布到真 marketplace。
- (旧候选搁置:对齐 AI 生成侧 / 演讲者同步 / Open-Design 画布。讲稿/双屏明确不做。)
- 全局蓝图与 N1-N5、复用/退役清单: `docs/PIVOT-v2-html-first.md`。契约: `docs/DECK-CONTRACT.md`。
- (旧 v1 候选,多数被 HTML-first 吸收/重排:)M8 收尾后,迭代候选(均为"如何迭代"已分析、用户暂选了"闭合AI视觉环"):**②打通人机协作环**(局部 patch 接口 + Studio 把某页回灌 AI 无损合并)、**③补内容表现力**(真图表/Studio 拖图/常用布局)、**④设计系统可拖入包**。自由拖拽 WYSIWYG 已决定缓做。
- 可选增强 M8: doctor 出"联系表"(缩略图拼图供一眼审)、自动修建议、把视觉审计也接进 Studio(人也能一键自检)。
- 持续沉淀新动效/主题进 `docs/design-system.md`(用户拿 AI 做新效果让我融入)。接口文档 `AGENTS.md`(agent)、`GUIDE.md`(人)。

## Read Next Only If Needed
- `NEXT-SESSION.md` · `decisions.md`(全决策) · `docs/design-system.md`(元素库登记册+怎么加新效果) · `AGENTS.md`(agent 接口) · `GUIDE.md`(人类指南) · `studio/README.md` · `docs/screenshots/m7/`
