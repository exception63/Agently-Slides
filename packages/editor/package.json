{
  "name": "@slidesmith/editor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Slidesmith editor: local live-preview + inline quick-edit GUI; reads/writes deck.json, reuses the render engine.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@slidesmith/ir": "*",
    "@slidesmith/engine": "*",
    "@slidesmith/parser-md": "*",
    "@slidesmith/themes": "*"
  }
}
